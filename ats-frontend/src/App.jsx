import { KanbanPage } from './pages/KanbanPage'

/**
 * App — raiz da aplicação.
 * Aqui é o lugar certo para adicionar futuramente:
 *   - React Router (múltiplas páginas)
 *   - Context providers globais (autenticação, tema)
 *   - Error boundaries
 */
export default function App() {
  return <KanbanPage />
}
